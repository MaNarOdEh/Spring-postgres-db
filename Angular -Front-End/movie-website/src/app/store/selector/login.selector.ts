import { LoginState } from "../Reducer/login.reducer";
import { createSelector } from "@ngrx/store";

//export const selectIsLogin = (state: LoginState) => state.isLogin;
//export const selectUserName = (state: LoginState) => state.isLogin;
//export const selectFeatureCount = createSelector(selectIsLogin,selectUserName);
